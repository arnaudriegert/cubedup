{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && jq -r '.tool_input.file_path' | xargs -I {} npx eslint --fix {} 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
